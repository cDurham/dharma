{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "cli",
  "projectType": "application",
  "sourceRoot": "tools/cli/src",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{workspaceRoot}/tools/cli/dist"],
      "options": {
        "command": "tsc --project tsconfig.json",
        "cwd": "tools/cli"
      }
    }
  }
}

