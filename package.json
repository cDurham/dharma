{
  "name": "dharma",
  "version": "1.0.0",
  "description": "Dharma - Full-stack application with NestJS backend and React frontend",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "db:reset": "cd backend && npm run db:reset",
    "db:sync": "cd backend && npm run db:sync",
    "db:seed": "cd backend && npm run seed",
    "codegen": "cd frontend && npm run codegen",
    "clean": "rm -rf backend/dist frontend/dist",
    "d:dev": "docker compose --profile dev up -d",
    "d:prod": "docker compose --profile prod up -d",
    "d:dev:rebuild": "docker compose --profile dev down && docker compose --profile dev build --no-cache && docker compose --profile dev up -d",
    "d:dev:rebuild:backend": "docker compose --profile dev down && docker compose --profile dev build backend-dev --no-cache && docker compose --profile dev up -d",
    "d:dev:restart": "docker compose --profile dev down && docker compose --profile dev up -d"
  },
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  },
  "keywords": [
    "nestjs",
    "react",
    "graphql",
    "typescript",
    "fullstack"
  ],
  "author": "",
  "license": "ISC"
}
