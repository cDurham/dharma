type LoginResponse {
  message: String!
}

input ValidateUserInput {
  email: String!
  password: String!
}

type Mutation {
  login(data: ValidateUserInput!): LoginResponse!
  refreshAccessToken: Boolean!
  logout: Boolean!
}
